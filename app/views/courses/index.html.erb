<% content_for :title do %>
  EDURGE-Jean | <%= action_name.humanize %> Courses
<% end %>

<div class="text-center">
  <h3 class="text-white">
    <i class="fa fa-graduation-cap"></i>
    <%= action_name.humanize %> courses
    <span class="badge badge-danger"><%= @courses.count %></span>
  </h3>
</div>

<div class="card-columns">
  <%= render 'courses/courses', courses: @courses %>
</div>
